LỊCH SPTV v0.1.3

Workflow tự chạy mỗi 5 phút bằng GitHub Actions:

  schedule:
    - cron: "*/5 * * * *"

Không cần cấu hình cron VPS hoặc cron-job.org.
GitHub có thể khởi chạy trễ khi runner đông.
Chạy thủ công vẫn dùng: Actions -> Update SPTV playlist -> Run workflow.
